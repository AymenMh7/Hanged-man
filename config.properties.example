# --------------------------------------------------------------------
# Pirate's Cove — MySQL connection settings (TEMPLATE)
#
# 1. Copy this file to "config.properties"  (same folder)
# 2. Edit db.user and db.password to match YOUR local MySQL install
# 3. Save — config.properties is git-ignored so your password stays
#    on your machine.
# --------------------------------------------------------------------

db.url=jdbc:mysql://localhost:3306/hangman_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=root
db.password=changeme
